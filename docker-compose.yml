version: "3.9"

services:


  qdrant:
    image: qdrant/qdrant
    ports:
      - "6333:6333"

  ollama:
    image: ollama/ollama
    ports:
      - "11434:11434"

  backend:
    build: .
    ports:
      - "8000:8000"

    depends_on:
      - qdrant
      - ollama

    volumes:
      - .:/app